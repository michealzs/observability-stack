# Blackbox exporter probe modules. Referenced from prometheus/prometheus.yml
# (blackbox-http uses http_2xx, blackbox-icmp uses icmp).

modules:
  http_2xx:
    prober: http
    timeout: 10s
    http:
      method: GET
      preferred_ip_protocol: ip4
      ip_protocol_fallback: true
      follow_redirects: true
      valid_http_versions: ["HTTP/1.1", "HTTP/2.0"]
      # Empty list means any 2xx status is a success.
      valid_status_codes: []
      fail_if_ssl: false
      fail_if_not_ssl: false
      tls_config:
        insecure_skip_verify: false

  icmp:
    prober: icmp
    timeout: 5s
    icmp:
      preferred_ip_protocol: ip4
      ip_protocol_fallback: true

  tcp_connect:
    prober: tcp
    timeout: 5s
    tcp:
      preferred_ip_protocol: ip4
      ip_protocol_fallback: true
